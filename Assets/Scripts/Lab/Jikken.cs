using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Jikken : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        int result = 2 / 10;
        Debug.Log("40 / 11 = " + result);
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
